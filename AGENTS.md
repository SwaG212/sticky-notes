# 项目级约定

- 接到代码新增、修改、调试或交互优化任务时，先阅读 `CODEMAP.md`，按功能索引定向定位；仅在调用链不明确时扩大阅读范围。
- 架构职责、目录或持久化格式发生变化时，同步更新 `CODEMAP.md`。
- 每次最终输出的最后附带本次任务调用的 skill。
- 使用了 skill 时，写明所有实际调用的 skill 名称，例如：`本次任务调用的 skill：ponytail、documents`。
- 未使用 skill 时，固定写：`本次任务未使用 skill`。
