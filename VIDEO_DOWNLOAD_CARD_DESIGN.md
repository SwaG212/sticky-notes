# 视频下载卡片效果稿

> 文档状态：待确认，不包含业务代码改动
> 所在页面：卡片页
> 视觉方向：延续现有便利贴的白底、浅灰控件、紫色主操作；紧凑、安静、可预期。

## 1. 目标

用户在卡片页粘贴一个可直接访问的视频链接，选择文件下载位置，确认后在卡片内查看实时下载进度；下载完成后可直接打开文件所在目录，下载完成或失败时均发送 Windows 系统通知。

卡片沿用项目现有 `tool-card` 外观，不引入视频预览，避免在小窗口内增加额外噪声。卡片高度随内容自然增高，超出窗口时由卡片页纵向滚动承载。

## 2. 卡片整体效果

以下线框按当前应用约 336px 的卡片内容宽度绘制。

### 2.1 初始状态 / 已粘贴链接、尚未下载

```text
┌──────────────────────────────────────────┐
│ 视频下载  Video Download                 │
│                                          │
│ 视频链接                                 │
│ ┌──────────────────────────────────────┐ │
│ │ https://v6-ipv6.toutiaovod.com/…    │ │
│ └──────────────────────────────────────┘ │
│                                          │
│ 文件下载位置                             │
│ ┌───────────────────────────────────┬──┐ │
│ │ D:\Downloads\Video               │▼ │ │
│ └───────────────────────────────────┴──┘ │
│                                          │
│ 等待确认                            0%   │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │  灰色静止
│                                          │
│ ┌──────────────────┐ ┌─────────────────┐ │
│ │   打开文件目录   │ │    确认下载     │ │
│ └──────────────────┘ └─────────────────┘ │
│   ↑ 灰色、不可点击       ↑ 紫色主按钮   │
└──────────────────────────────────────────┘
```

视觉说明：

- “视频链接”采用现有工具卡输入框样式；超长链接单行省略，悬停显示完整链接。
- “文件下载位置”复用设置页文件存储位置的组合框语言：文本输入框、右侧下拉箭头、历史路径浮层。
- 未下载时进度轨道和进度填充均为灰色，不显示流动动画。
- 左侧固定为“打开文件目录”，右侧固定为下载主操作，避免状态切换时按钮位置跳动。
- “确认下载”使用紫色主按钮；“打开文件目录”在下载完成前为灰色禁用态。

### 2.2 下载位置下拉框展开

```text
│ 文件下载位置                             │
│ ┌───────────────────────────────────┬──┐ │
│ │ D:\Downloads\Video               │▲ │ │
│ └───────────────────────────────────┴──┘ │
│ ┌──────────────────────────────────────┐ │
│ │ 默认：系统下载目录                  │ │
│ │ D:\Downloads\Video                 │ │
│ │ E:\临时下载                         │ │
│ └──────────────────────────────────────┘ │
```

- 最多显示 5 条最近使用路径，当前项可显示轻量选中标记。
- 路径过长时单行省略，悬停显示完整路径。
- 支持键盘上下键选择、Enter 确认、Esc 关闭。

### 2.3 下载中

```text
┌──────────────────────────────────────────┐
│ 视频下载  Video Download                 │
│                                          │
│ 视频链接                                 │
│ ┌──────────────────────────────────────┐ │
│ │ https://v6-ipv6.toutiaovod.com/…    │ │
│ └──────────────────────────────────────┘ │
│                                          │
│ 文件下载位置                             │
│ ┌───────────────────────────────────┬──┐ │
│ │ D:\Downloads\Video               │▼ │ │
│ └───────────────────────────────────┴──┘ │
│                                          │
│ 正在下载 video_20260903.mp4       42%   │
│ ━━━━━━━━━━━━━━━╺━━━━━━━━━━━━━━━━━━━━━━━ │  紫色实线进度
│ 18.6 MB / 44.2 MB              1.8 MB/s │
│                                          │
│ ┌──────────────────┐ ┌─────────────────┐ │
│ │   打开文件目录   │ │    下载中…      │ │
│ └──────────────────┘ └─────────────────┘ │
│       ↑ 仍不可点击       ↑ 不可重复点击 │
└──────────────────────────────────────────┘
```

- 下载期间锁定链接和下载路径，防止显示内容与实际任务不一致。
- 进度条使用紫色实线从左向右增长，不使用循环跑马灯。
- 服务端未提供总大小时，百分比显示为 `--`，进度条改为低调的紫色呼吸态，并继续显示已下载大小。

### 2.4 下载完成

```text
│ 已完成 video_20260903.mp4         100%  │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │  绿色完成态
│ 44.2 MB · 已保存到 D:\Downloads\Video   │
│                                          │
│ ┌──────────────────┐ ┌─────────────────┐ │
│ │   打开文件目录   │ │    再次下载     │ │
│ └──────────────────┘ └─────────────────┘ │
│       ↑ 可点击                           │
```

- 完成后进度条变为绿色，并保留文件名、文件大小和实际保存目录。
- “打开文件目录”启用，点击后在 Windows 文件资源管理器中打开目录并选中下载文件。
- 下载完成时立即发送一次 Windows 系统通知；即使便利贴窗口处于隐藏状态也会显示。
- 用户修改链接或路径后，卡片重置为“等待确认”，进度条恢复静止灰色。

Windows 成功通知：

```text
┌──────────────────────────────────────────┐
│ 便利贴                                   │
│ 视频下载完成                             │
│ video_20260903.mp4 已保存                │
│                           [打开文件目录] │
└──────────────────────────────────────────┘
```

- 点击通知正文或“打开文件目录”，打开文件资源管理器并选中已下载文件。

### 2.5 下载失败

```text
│ 下载失败：链接已失效或服务器拒绝访问     │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │  灰色轨道
│                                          │
│ ┌──────────────────┐ ┌─────────────────┐ │
│ │   打开文件目录   │ │    重新下载     │ │
│ └──────────────────┘ └─────────────────┘ │
```

- 错误文字使用现有危险色，尽量给出可操作原因，例如“目录不可写”“磁盘空间不足”“链接已失效”。
- 失败时保留链接和路径，方便用户直接重试；没有完整文件时，“打开文件目录”保持禁用。
- 下载失败时立即发送一次 Windows 系统通知，通知正文展示精简后的失败原因。

Windows 失败通知：

```text
┌──────────────────────────────────────────┐
│ 便利贴                                   │
│ 视频下载失败                             │
│ 链接已失效或服务器拒绝访问               │
│                               [查看详情] │
└──────────────────────────────────────────┘
```

- 点击失败通知，将便利贴窗口唤出并定位到视频下载卡片，便于修改链接或重新下载。
- 同一次下载任务只发送一条最终通知，避免底层重复错误事件造成通知轰炸。

## 3. 控件与状态规则

| 状态 | 链接输入 | 路径选择 | 确认按钮 | 打开文件目录 | 进度条 |
|---|---|---|---|---|---|
| 空白 | 可输入 | 可选择 | 禁用 | 禁用 | 灰色静止，0% |
| 等待确认 | 可编辑 | 可编辑 | 启用，“确认下载” | 禁用 | 灰色静止，0% |
| 下载中 | 锁定 | 锁定 | 禁用，“下载中…” | 禁用 | 紫色实时进度 |
| 已完成 | 可编辑 | 可编辑 | 启用，“再次下载” | 启用 | 绿色，100% |
| 下载失败 | 可编辑 | 可编辑 | 启用，“重新下载” | 禁用 | 灰色静止，保留错误文案 |

## 4. 建议交互流程

```text
粘贴链接
  → 校验为 http/https 直链
  → 选择或输入下载目录
  → 点击“确认下载”
  → 校验目录可写并确定安全文件名
  → 实时回传进度
  ├─ 成功 → 原子完成文件落盘 → 启用“打开文件目录” → Windows 成功通知
  └─ 失败 → 保留链接与路径 → 显示可操作错误 → Windows 失败通知
```

补充规则：

- 文件名优先取响应头 `Content-Disposition`，其次取 URL 路径；无法取得时使用 `video_YYYYMMDD_HHmmss.mp4`。
- 文件扩展名优先依据响应类型；示例链接的 `video_mp4` 保存为 `.mp4`。
- 同名文件默认不覆盖，自动追加 ` (1)`、` (2)`。
- 只允许 `http:` 和 `https:`，拒绝 `file:` 等本地或自定义协议。
- 下载中的临时文件使用 `.part` 后缀；成功后再改为最终文件名，失败时清理临时文件。
- 卡片只展示当前一次下载，不在本卡片内增加任务队列或历史列表。

## 5. 与现有产品的衔接

- 卡片通过现有 `createToolCard('video-download', 'video-download-card')` 创建。
- 卡片页设置中建议在“翻译”和“DeepSeek Harness”同级增加“视频下载”启用开关。
- 下载目录使用独立配置，不复用笔记的 `notesDir`；建议字段为 `videoDownloadDir` 和最近 5 条 `videoDownloadDirHistory`。
- 渲染层只负责展示与发送命令；网络下载、路径校验、文件写入、打开资源管理器均由主进程完成。
- 主进程只向受信任渲染页面开放最小 IPC，并对 URL、目录、文件名和重定向后的协议再次校验。
- Windows 通知由主进程创建；成功通知携带最终文件路径，失败通知携带当前卡片定位信息。

## 6. 验收要点

- 粘贴题述超长视频链接后，链接输入框不撑破卡片。
- 未开始时进度条为完整灰色且完全静止。
- 点击确认后，百分比与已下载字节实时增长，重复点击不会创建第二个下载。
- 下载完成后，“打开文件目录”可用，并能打开目录、选中正确文件；同时只弹出一条 Windows 成功通知。
- 目标目录不可写、链接无效、网络断开时有明确错误，界面可重试；同时只弹出一条 Windows 失败通知。
- 点击成功通知能打开目录并选中文件；点击失败通知能唤出应用并定位到视频下载卡片。
- 卡片增高后不遮挡底部返回按钮，卡片页可正常纵向滚动。
- 下拉框、按钮均支持键盘操作，禁用态和焦点态清晰可见。

## 7. 需要确认的产品决策

1. 需求中的“随机延长卡片上下长度”，是否实际指“卡片随内容自动增高”？本稿按自动增高处理，不做随机高度。
2. 默认下载目录是否使用 Windows 系统“下载”目录，并单独记忆最近 5 个视频下载目录？
3. 下载过程中是否需要“取消下载”能力？本稿暂不增加；下载中“确认下载”仅变为禁用的“下载中…”。
4. 一张卡片是否只允许同时下载一个视频？本稿按单任务处理，不设计队列。
5. 应用重启后是否需要恢复未完成下载或保留上一次完成记录？本稿按不恢复处理，重启后回到初始状态。
6. “打开文件目录”是否需要在下载完成前也可点击、仅打开当前选择的目录？本稿严格按需求，完成前禁用，完成后打开并选中文件。
